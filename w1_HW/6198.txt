6198번: 옥상 정원 꾸미기

1. 
N개의 빌딩. 각 빌딩의 높이 hi. 
각 빌딩의 옥상 정원 관리인이 볼 수 있는 다른 건물의 옥상 정원 수를 모두 더한다.
자신의 위치한 빌딩보다 높거나 같은 빌딩이 있으면 그 다음 모든 빌딩은 보이지 않음.
+자신의 빌딩 좌측은 따로 확인하지 않는 듯 함...(우측만 고려)

2. 
각 빌딩 마다 반복. (빌딩 i)
stack에 존재하는 빌딩 중 빌딩 i보다 낮은 빌딩 제거.
빌딩 i를 stack에 추가.
stack에 남아있는 빌딩들 - 1을 합계에 추가.

3.
int n = 빌딩 개수
int[] building = 빌딩들의 높이
int result = 0; //출력될 합계
stack 생성.

for(n){
    while(stack.peek <= 현재 빌딩 높이){
        stack.pop
    }
    stack.push(현재 빌딩 높이);
    total += stack.size - 1;
}
print(total);

-> 틀렸습니다.
해결 : result를 int -> long으로 변경.
빌딩의 개수N 은 1 <= N <= 80,000
각 빌딩의 높이 hi 는 1 <= hi <= 1,000,000,000
이었다는 것을 고려.
